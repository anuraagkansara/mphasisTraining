SELECT EMP.FIRST_NAME||' '||EMP.LAST_NAME||' '||' WORKS IN '||DEPT.DEPARTMENT_NAME||' LOCATED AT '||LOC.CITY
FROM EMPLOYEES EMP JOIN DEPARTMENTS DEPT 
ON EMP.DEPARTMENT_ID = DEPT.DEPARTMENT_ID
JOIN LOCATIONS LOC
ON LOC.LOCATION_ID = DEPT.LOCATION_ID
/
